# Dependencias del generador de reporte MBE
# Instalar con: pip install -r report_generator/requirements.txt

sentence-transformers>=2.7.0   # embeddings multilingues (paraphrase-multilingual-MiniLM-L12-v2)
umap-learn>=0.5.6              # reduccion de dimensiones para clustering
scikit-learn>=1.3.0            # HDBSCAN (>=1.3), KMeans, preprocesamiento
plotly>=5.22.0                 # graficos interactivos embebidos en HTML
jinja2>=3.1.4                  # motor de plantillas HTML
pandas>=2.0.0                  # manipulacion de datos
numpy>=1.24.0                  # operaciones numericas
groq>=0.9.0                    # LLM para nombrar grupos tematicos

# Si scikit-learn < 1.3 (sin HDBSCAN integrado), instalar tambien:
# hdbscan>=0.8.33
